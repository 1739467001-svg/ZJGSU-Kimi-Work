# 「商大元境」3D 视觉模拟构建实施手册 v1.0

> 配套文档:`PRD_CampusTwinX_商大元境_3D智能孪生平台.md`(蓝图级)＋`可行性评估_CampusTwinX.md`(裁剪口径)
> 本手册是**手册级**实现指南:每个模块给出 目标 → 数据 → 代码骨架 → 参数表 → 验收 → 坑。
> 读者:Kimi K3 结对编程。代码骨架为 TypeScript + react-three-fiber(R3F),可直接开工补全。
> 范围:M0(数据地基)→ M2(宏大观感)。M1(UI/业务闭环)只给接口约定。

---

## 0. 手册使用方式

1. 严格按 §7 逐日任务卡推进;每完成一个模块,先过该模块的「验收」再进下一个。
2. 所有代码骨架里的 `TODO` 是留给实现者的唯一空白;骨架之外的文件不要提前创建。
3. 遇到与 PRD 冲突时:以本手册为准(它已吸收可行性评估的 10 条修正)。
4. 性能红线(≤150 万三角面 / ≤300 DrawCall / 中端机 60FPS)在每次提交前用 §8.1 清单自查。

---

## 1. 构建总流程

```txt
OpenStreetMap (Overpass API, WGS-84)
   │  fetch-osm.mjs            产出 raw-osm.json
   ▼
红线裁剪 + 邻校清洗            clip-campus.mjs   产出 campus-geo.json
   │  (campus-redline.geojson 手工绘制,一次性)
   ▼
坐标烘焙 + 元数据合并          bake-geometry.mjs 产出 public/data/campus/*.json
   │  (WGS-84 → 校区中心局部米制;合并 buildings-meta.json 的 name/height/lod/zone)
   ▼
运行时装配                     SceneRoot.tsx
   │  (按 zone 分块懒加载;体块拉伸在运行时一次性完成并合并)
   ▼
氛围层 + 业务状态驱动          Atmosphere / 效果配方(§4)
```

**坐标换算(唯一公式,全项目统一放在 `lib/geo.ts`)**

```ts
// lib/geo.ts
export const ORIGIN = { lat: 30.309378, lng: 120.388317 } // 综合大楼锚点(WGS-84 化后)
const M_PER_DEG_LAT = 110540
const M_PER_DEG_LNG = 111320 * Math.cos((ORIGIN.lat * Math.PI) / 180)

export function toLocal(lng: number, lat: number): [number, number] {
  return [
    (lng - ORIGIN.lng) * M_PER_DEG_LNG,   // x,向东为正
    -((lat - ORIGIN.lat) * M_PER_DEG_LAT) // z,向南为正(three.js 里 z 向南更符合直觉)
  ]
}
// 海拔/高度一律用米,three.js 里即 y 值
```

> 坑:GCJ-02 锚点必须先转 WGS-84 再当 ORIGIN;转换用 `coordtransform`(仅 ETL 阶段用,运行时不依赖)。

---

## 2. M0 数据地基(4–6 天)

### 2.1 `tools/etl/fetch-osm.mjs` — 拉取

目标:拿到校区全域的 building / highway / water / leisure / natural 原始数据。

```js
// tools/etl/fetch-osm.mjs
const BBOX = '30.298,120.372,30.320,120.402' // s,w,n,e —— 覆盖教学区+三大生活区
const MIRRORS = [
  'https://overpass.kumi.systems/api/interpreter',
  'https://lz4.overpass-api.de/api/interpreter',
  'https://overpass-api.de/api/interpreter',
]
const QUERY = `
[out:json][timeout:120];
(
  way[building](${BBOX});
  relation[building](${BBOX});
  way[highway](${BBOX});
  way[natural=water](${BBOX});
  relation[natural=water](${BBOX});
  way[waterway](${BBOX});
  way[leisure](${BBOX});
  node[natural=tree](${BBOX});
  way[landuse](${BBOX});
);
out geom;`
// TODO: 依次尝试 MIRRORS,失败指数退避重试(最多 6 次);成功写 raw-osm.json
```

验收:`raw-osm.json` ≥ 8MB 量级;建筑 way ≥ 400;道路 way ≥ 400。
坑:镜像常 429/504,必须多镜像+退避;`out geom` 直接带节点坐标,省去二次解析。

### 2.2 `tools/etl/campus-redline.geojson` — 手工红线(一次性,约 2 小时)

- 用 geojson.io 画 4 个多边形:`teaching`(教学区围墙内)、`life_qianjiangwan`、`life_jinshagang`、`life_yupingzhou`,各带 `properties.zone`。
- 画法依据:官方研招网考点布局示意图(yjszs.zjgsu.edu.cn/2025/1219/c469a209748/page.htm)+ 高德卫星图对照。

### 2.3 `tools/etl/clip-campus.mjs` — 裁剪与清洗

```js
// 依赖:@turf/turf(仅 devDependencies)
// 1. 每个 building way 取 footprint 中心点,point-in-polygon 判定落入哪个红线区 → 打 zone 标签
// 2. 不落任何红线区 → 标记 outside:true(保留作 L3 远景,但剔除 name)
// 3. 白名单清洗:在 buildings-meta.json 白名单内的实名保留;白名单外的 name 一律删除
//    (防浙财大/中学/校外混入的铁律,见 PRD §8.2)
// 4. 道路/水系/绿地:按与红线相交关系裁剪(turf.booleanIntersects),同样打 zone
// TODO: 实现并写 campus-geo.json
```

验收:教学区内命名建筑 ≥ 85 栋且全部能在官方示意图上指认;`outside` 建筑无一名牌。

### 2.4 `tools/etl/buildings-meta.json` — 元数据表(1–2 天人工)

```jsonc
{
  "w123456789": {                        // key = OSM way id
    "name": "信电楼",
    "alias": ["信息与电子工程学院楼", "新电学院综合楼", "信电学院"],
    "zone": "south_colleges",
    "lod": 1,
    "levels": 6,                          // 人工定高来源:基建处档案 > 卫星图影子估算 > 类型默认
    "heightM": 24.0,                      // 有确切值填确切值,无则留空走估算引擎
    "feature": "college",                 // teaching|college|library|admin|venue|sport|dorm|canteen|service
    "hero": false
  }
}
```

填写优先级:① 基建处工程档案(jjc.zjgsu.edu.cn)② 官方示意图+卫星图 ③ 类型默认。附录 A(PRD)即初始内容。

### 2.5 高度估算引擎 `lib/heightEstimate.ts`

```ts
const FLOOR_H: Record<string, number> = {
  teaching: 3.6, college: 3.6, library: 4.2, admin: 3.6,
  venue: 6.0, sport: 8.0, dorm: 3.0, canteen: 4.5, service: 3.0,
}
const DEFAULT_LEVELS: Record<string, number> = {
  teaching: 5, college: 5, library: 8, admin: 12,
  venue: 2, sport: 2, dorm: 6, canteen: 3, service: 1,
}
export function estimateHeight(meta: { levels?: number; heightM?: number; feature: string }): number {
  if (meta.heightM) return meta.heightM
  const levels = meta.levels ?? DEFAULT_LEVELS[meta.feature] ?? 4
  return levels * (FLOOR_H[meta.feature] ?? 3.2)
}
// 校验:Σ(footprint面积 × levels) 分区汇总,与规划 PDF 分区总建筑面积对表,偏差 >20% 的区复查
```

### 2.6 `tools/etl/bake-geometry.mjs` — 烘焙产物

产出(全部进 `public/data/campus/`):

| 文件 | 内容 | 格式要点 |
|---|---|---|
| `buildings.json` | 全部楼宇:footprint(局部米制)、height、name、zone、lod、feature | 体块**运行时**拉伸,不在 ETL 固化(便于改高调试) |
| `roads.json` | 道路中心线折线 + width + name | 校内实名道路标 `named:true` |
| `water.json` | 水面多边形(墨湖/月亮湾/出蓝园/河道) | 墨湖标 `hero:true` |
| `green.json` | 绿地/花海/运动场面 | — |
| `trees.json` | 树点位(OSM tree + 沿道路/绿地程序化补点规则) | 见 §3.5 |
| `landmarks.json` | 18 个地标:position、kind、story(供 RAG) | 手工点位 |

验收:任何 JSON 可直接 `fetch` 加载;抽查 10 栋楼相对位置与官方示意图一致;综合大楼与锚点误差 <5m。

---

## 3. 场景组装(SceneRoot)— 静态世界

### 3.1 分块加载

```tsx
// components/campus3d/SceneRoot.tsx
const ZONES = ['teaching_core', 'west_sports', 'south_colleges', 'life_areas', 'outside_ring'] as const
// teaching_core 首屏必载;其余 useMemo + <Suspense> 按相机距离挂载;
// outside_ring 仅 L3 灰盒,最后加载,可整体跳过(低配档)
```

### 3.2 楼宇体块 `BuildingMesh.tsx` — 全项目最重要的性能决策

```tsx
// 策略:L1/L2/L3 → 按 feature 分材质合并成少量大 Mesh(mergeGeometries);
//       L0 + 需要交互(点击/剖层)的楼 → 独立 Mesh。
import { mergeGeometries } from 'three/examples/jsm/utils/BufferGeometryUtils.js'

function buildingGeometry(b: Building): BufferGeometry {
  const shape = new THREE.Shape(b.footprint.map(([x, z]) => new THREE.Vector2(x, z)))
  const g = new THREE.ExtrudeGeometry(shape, { depth: b.height, bevelEnabled: false })
  g.rotateX(-Math.PI / 2) // footprint 在 xz 平面,向 +y 拉伸
  g.translate(0, 0, 0)
  return g
}
// 状态着色:不用每楼一个 material,用 vertex color —— 拉伸时给每楼几何体写满
// 同一 color attribute,合并后用一个支持 vertexColors 的材质;
// 状态变化时只改该楼顶点色 buffer(不用重建几何)。
```

坑:ExtrudeGeometry 的 shape 必须统一顺时针/逆时针,否则法线翻黑;合并前 `geometry.computeVertexNormals()`;每楼顶点数大时按 `feature` 分桶合并,DrawCall ≈ 材质数(3–5)。

### 3.3 地面 `GroundPlate.tsx`

```txt
基底:深色低饱和大盘(比校园红线大 1.3 倍),#1a1f24(沉浸暗色基调)
绿地:green.json 多边形 → ShapeGeometry 贴地(y=0.02),低饱和绿 #2e4034
道路:roads.json → 条带网格(见 3.4),沥青灰 #3a3f45,校内路略浅
标线:实名道路中心线上叠淡色虚线(shader UV 滚动 = 车流光带,见 §4.7)
```

### 3.4 道路条带 `RoadNetwork.tsx`

```ts
// 折线 → 三角条带:对每个线段求法向,左右各扩 width/2
function ribbon(points: [number, number][], width: number): BufferGeometry {
  // TODO: 逐点计算切线→法向→左右顶点→索引;接缝处取平均法向
  // 100 条校内道路总顶点 < 50k,一次合并
}
```

### 3.5 植被 `Vegetation.tsx`

```txt
点位来源:① OSM natural=tree 实测点;② 程序化补点:沿 named 道路两侧每 15m 一棵行道树,
         green 多边形内按密度 poisson 撒点(种子固定,保证可复现)
模型:2 棵低模树(阔叶/针叶,各 <300 面),Blender 或手写 cone+sphere 组合
渲染:InstancedMesh × 2;近景树(相机 200m 内)用实体,远景同批实例换低模公告板(降级档)
四季:setColorAt 改 instanceColor(春 #7fae5a / 夏 #4d7c43 / 秋 #c98f3d / 冬 #6b5d4f 且缩放 y*0.9)
```

### 3.6 水面 `WaterSystem.tsx`(按可行性修正:墨湖单湖真反射)

```tsx
// 墨湖(hero:true):three/examples Water —— 一个 Reflector 渲染目标
import { Water } from 'three/examples/jsm/objects/Water.js'
// waterNormals 贴图:自建 256px 法线图或 three 自带 waternormals.jpg(需本地化,断网!)
// 其余水面:MeshStandardMaterial + onBeforeCompile 注入 UV 滚动法线采样(伪反射,零渲染目标)
// 河道:同上伪反射 + 流向 UV 滚动
```

坑:Water.js 的反射相机在昼夜切换时要同步光照;低配档把墨湖也降为伪反射(一个开关)。

### 3.7 名牌与标注 `SceneLabels.tsx`

- L0/L1 楼名:drei `Text`(troika-three-text,SDF 清晰), LOD:相机 >400m 只显示 L0;>800m 全隐藏。
- 地标小图标:drei `Html` 限量(≤12 个同屏),超出距离自动隐藏。Html 数量是性能杀手,严控。

---

## 4. 氛围与效果配方(14 个,每个含代码骨架)

> 通用约定:所有效果参数挂在 `uiStore.quality` 三档上;所有动画驱动用 `useFrame`,禁止 setInterval;所有 shader 改动用 `onBeforeCompile` 注入,不新建 ShaderMaterial 除非必须。

### 4.1 昼夜循环(性价比之王,最先做)

```ts
// lib/sun.ts
import SunCalc from 'suncalc'
export function sunDirection(date: Date, lat = 30.309378, lng = 120.388317) {
  const { azimuth, altitude } = SunCalc.getPosition(date, lat, lng)
  return new THREE.Vector3(
    Math.sin(azimuth) * Math.cos(altitude),
    Math.sin(altitude),
    Math.cos(azimuth) * Math.cos(altitude)
  ).normalize()
}
```

```tsx
// Atmosphere.tsx:simClock.date → sunDirection → <directionalLight position>
// 白天:<Sky sunPosition> / 夜晚:<Stars> + 环境光压暗 + 窗灯(4.2) + 路灯实例点亮
// 时间压缩:simStore.speed(默认 60x);scene_director 意图可锁定任意时刻
// 过渡:太阳高度角 <0 的 20 分钟内做黄昏插值(灯光 ler皮、天空色调暖)
```

验收:锁定 2026-07-17 18:40(杭州夏日黄昏)肉眼可见晚霞;21:30 图书馆灯渐熄(仿真事件)。

### 4.2 夜景窗灯 `WindowLights.tsx`

```txt
思路:不为每扇窗建模——L1/L2 楼宇立面贴图本身就是窗户平铺 UV;
     夜景 = 同一张贴图的 emissive 副本,随机变暗 60% 窗格
实现:程序化生成 2 张 CanvasTexture(窗格阵列 + 随机灭灯种子),材质 emissiveMap,
     emissiveIntensity 随太阳高度角 0→1
L0 地标:独立贴图(图书馆整面玻璃幕墙暖光、文体中心幕墙流光见 §5.1)
成本:0 额外 DrawCall(材质复用),纯贴图
```

### 4.3 雨/雪 `WeatherParticles.tsx`

```tsx
// 相机跟随圆柱域(半径 80m 高 60m),域内 2000(雨)/1500(雪)个 InstancedMesh 小片
// useFrame:实例 y -= speed*dt;落到 y<0 回收到顶部;域随相机平移(不旋转)
// 雨:细长条 + 速度 20m/s + 轻微斜风;雪:小圆片 + 1.5m/s + 水平摆动 sin(t+id)
// 低档:数量减半 + 关闭摆动
```

### 4.4 雾

```tsx
<fogExp2 attach="fog" args={['#9db3c8', 0.0016]} /> // 场景 attach
// 雾天意图:density 0.0016→0.006 插值 2 秒;配合太阳亮度压 40%
```

### 4.5 四季

```txt
春/夏/秋/冬 = 树 instanceColor 四套 + 花海显隐(春油菜/夏秋向日葵)+ 冬季树 y 缩放 0.9 + 雪叠加
触发:scene_director「看看秋天的商大」→ season:'autumn' → 2 秒颜色 lerp
```

### 4.6 白鸽粒子 `Doves.tsx`(彩蛋)

```tsx
// 鸽子广场固定点位,12 只 InstanceMesh 小鸟(2 三角面身体)
// 常态:地面随机踱步(位置抖动);点击广场或整点 → 起飞:沿各自螺旋线上升→绕广场 2 圈→落回
// 全部在 useFrame 内以 id 为相位驱动,无物理引擎
```

### 4.7 车流/人流光带 `FlowLines.tsx`

```tsx
// 车流:named 道路中心线 → shader 材质,UV.x 随时间滚动,亮斑 = 车;
//      密度 uniform 由 simStore(时段)给:早高峰 0.8 / 深夜 0.05
// 人流:教学区↔生活区 5 条主干路径(CatmullRomCurve3),每条 200 个光点 Points,
//      点沿曲线参数 t 循环流动,密度=潮汐函数(课间脉冲、午间食堂、晚间图书馆)
// 关键:光点用 additive blending,尺寸随距离衰减,黑夜下效果最佳(与 4.1 联动的理由)
```

### 4.8 Agent 扫描光(Wow 1 的灵魂)

```tsx
// 一块覆盖全校的透明平面(y=0.5),shader:一道 30m 宽的亮带沿 x 从 -700 → +700 扫过
// 亮带经过处,楼宇 vertexColor 临时提亮(传入扫描 x 到楼宇材质的 uniform,按楼心 x 距离衰减)
// 时序:sceneMode='searching' 触发,1.2 秒扫完 → 候选楼点亮(蓝) → CameraDirector 推镜
```

### 4.9 候选高亮与选中描边

```tsx
// @react-three/postprocessing:<Outline selection> 需要独立 Mesh —— L0/候选楼本来独立,直接用
// 候选蓝色(brand #3aa7ff)Outline + 轻微 Bloom;选中金色 #e8b84b
// 合并进大 Mesh 的 L2 房间不可描边 → 用房顶浮出小色块(positionHint)代替
```

### 4.10 楼宇剖层 `BuildingSlice.tsx`

```tsx
// 仅综合大楼(12 层)+ 1 栋教学楼做真剖层:
// 楼体按楼层拆成 12 个独立薄 Mesh(构建时切好,不是运行时切)
// 触发 slicedBuildingId → 非目标层 material.opacity → 0.12,目标层 y 方向错开 1.5m 展开
// 目标房间 = 该层内小 box(positionHint 布点)发光描边 + Html 信息卡
// 其余楼"伪剖层":整楼透明度 0.3 + 目标房间浮出,不拆层(成本为 0)
```

### 4.11 报修红色脉冲

```tsx
// 目标房间小 box:emissive #ff3b30,useFrame 里 intensity = 2 + sin(t*4)*1.5
// 楼顶告警标记:Sprite(三角叹号贴图)上下浮动 + 楼体灯带局部红(StatusLightBand 段染色)
```

### 4.12 热力层 `HeatLayer.tsx`

```txt
数据:按楼的占用率/能耗/人流 0–1
实现 A(推荐):直接改楼体 vertexColor(冷→暖 lerp),零额外几何
实现 B(观感更好):每楼 footprint 上方 0.3m 放半透明色块合并成一层,opacity 0.35
镜头:overview 模式 CameraDirector 拉 45° 俯视
```

### 4.13 首屏开场 `OpeningSequence.tsx`(5 秒保底版 / 12 秒完整版)

```tsx
// 一个 Timeline 状态机:keyframes = [{t, camPos, lookAt, event}]
// 完整版:江面雾(0s)→ 学术轴低飞(2s,楼逐栋点亮=vertexColor 随镜头 x 推进)
//        → 墨湖掠过(6s)→ 鸽子起飞(7s)→ 中心广场拉升(8s)→ 全景+标题(10s)
// 实现:一条 CatmullRomCurve3 预烘焙曲线,t 参数驱动;期间禁用 OrbitControls,点击/按键跳过
// 保底版:直接用完整版的 8s→12s 段(拉升+全景),砍前段
```

### 4.14 疏散推演(P1)`DrillSim.tsx`

```tsx
// 数据:目标楼出口点(手工标 2–4 个)+ 楼到集合点的路径折线(手工标,不做自动寻路!成本砍 90%)
// 人群:500 个光点从楼层房间 → 出口 → 路径 → 集合点,速度 1.2m/s,出口处限流排队(参数)
// 染色:楼层危险度 = f(起火层, 时间),vertexColor 红化;路径绿色流光
// 产出卡:疏散用时、瓶颈出口、建议(规则生成)
```

---

## 5. L0 地标建模分解(风格化意象,不追求测绘级)

### 5.1 文体中心(亚运手球馆)—— 难度最高,单独 1.5 天

```txt
形态依据:太阳花造型主馆 + 副馆 + 东看台;392 条水幔幕墙
体块分解:
  ① 基座:八边形圆台(CylinderGeometry 8 段),r=55m,h=6m
  ② 花瓣屋顶:12 片"花瓣"——每片为拉伸的弧形板(自定义 Shape:椭圆弧 → ExtrudeGeometry),
    绕中心旋转阵列,端部上翘(每片 y 旋转微倾),形成向日葵剪影
  ③ 水幔幕墙:不做 392 根几何!圆柱侧面玻璃材质 + shader 竖向条纹(UV.x 细分 392 条,
    夜景条纹内流光滚动)——一眼"水幔",成本为 0
  ④ 副馆:半圆柱拱顶;东看台:阶梯 box 组
夜景:幕墙流光 + 顶部花瓣边缘点光
验收:200m 外剪影可辨"太阳花";近看幕墙有竖向肌理;三角面 < 15k
```

### 5.2 综合大楼(12 层圆形主楼)—— 0.5 天

```txt
① 主塔:CylinderGeometry(r=28m, h=48m, 48 段)+ 顶部设备层小圆台
② 裙楼:2 层方形环楼(4 个 box 围合),与主塔相切
③ 立面:玻璃幕墙贴图(竖向分格),楼层线 = 贴图横带
④ 剖层支持:塔体按 12 层预切成 12 段(见 §4.10)
⑤ 门前金字塔标志物:独立小锥体 + 名牌
```

### 5.3 图书馆 —— 0.5 天

```txt
① 体量:42,000㎡馆舍 → 主体大板楼(约 110m×45m×8 层,以 OSM 轮廓为准拉伸后加屋顶收分)
② 特征:临墨湖一面全玻璃幕墙(高反射材质,映湖),入口大台阶(3 个叠放 box)
③ 屋顶:平屋顶 + 女儿墙 + 顶部名牌「图书馆」
④ 夜景:整面幕墙暖光,21:30 渐熄(仿真联动)
⚠ 以卫星图校准轮廓,形态未知部分从简——它是"大",不是"奇"
```

### 5.4 飞翔门 + 凯旋门 —— 各 0.5 天

```txt
飞翔门(南大门,主入口,飞鸟意象):
  ① 两根 12m 门柱(倒角 box)
  ② 门楣 = 展翅形态:自定义 Shape(对称翼型曲线)→ ExtrudeGeometry,中部上拱
  ③ 校名匾 = Text 面板
凯旋门(北大门):
  ① 简化凯旋门体量:中央拱洞(box 拼合挖洞 = 3 个 box)+ 顶部檐口
  ② 与飞翔门统一材质(米白石材贴图)
```

---

## 6. 资产与贴图管线

```txt
贴图策略(全部程序化生成或本地化,断网红线):
  facade-teaching.webp   教学楼窗格平铺(白天)
  facade-teaching-night.webp 同布局夜景 emissive
  facade-dorm.webp / facade-canteen.webp / glass-curtain.webp
  waternormals.jpg(three 自带,拷进 public/textures)
  tree 两张 256px
生成工具:tools/etl/gen-textures.mjs(node-canvas 或直接 SVG 转 PNG),512px 上限,WebP q80
压缩:gltf-transform optimize in.glb out.glb --compress meshopt --texture-compress webp
命名:bldg_<osmid>.glb(L0 才单文件);其余全部 JSON 驱动,无 glb
```

---

## 7. 逐日任务卡(M0 → M2,单人 + AI 全职当量)

### M0 数据地基(5 天)

| 日 | 任务 | 验收 |
|---|---|---|
| D1 | fetch-osm + 多镜像重试;raw-osm.json 落盘 | 建筑 ≥400 / 道路 ≥400 |
| D2 | 红线绘制(2h)+ clip-campus 裁剪清洗 | 教学区实名 ≥85 栋,零邻校名牌 |
| D3 | buildings-meta 白名单(附录 A 录入,约 90 栋)+ 高度估算引擎 | 抽查 10 栋层数合理;分区面积校验偏差 <20% |
| D4 | bake-geometry 六类产物 + 锚点回归(综合大楼 <5m) | 全部 JSON 可 fetch |
| D5 | 灰盒场景:BuildingMesh 合并渲染 + 轨道相机 + 点击取楼名 | 鸟瞰全校区不卡,名字全对 |

### M1 业务闭环(5–7 天,3D 只做接口)

| 日 | 任务 |
|---|---|
| D1–2 | 双模式 UI 骨架 + Zustand 三 store |
| D3–4 | 规则引擎 12 意图(先 P0 六意图)+ Agent 时间线 |
| D5–6 | 预约/报修闭环(列表式,不依赖 3D)+ demoScenarios |
| D7 | 3D 联调接口:highlightRooms/focusCamera/setSceneMode 三接口打通 |

### M2 宏大观感(10 天)

| 日 | 任务 | 涉及配方 |
|---|---|---|
| D1 | 地面/道路/绿地/水系(墨湖真反射) | §3.3–3.6 |
| D2 | 立面贴图 + 窗格平铺 + L1/L2 合批 | §3.2、§6 |
| D3 | 植被(行道树+绿地撒点)+ 名牌 | §3.5、§3.7 |
| D4 | 昼夜循环 + 黄昏过渡 | §4.1 |
| D5 | 夜景窗灯 + 路灯 + 图书馆 21:30 熄灯 | §4.2 |
| D6 | L0:综合大楼 + 图书馆 | §5.2、§5.3 |
| D7 | L0:文体中心(太阳花+水幔 shader)+ 双门 | §5.1、§5.4 |
| D8 | 首屏开场(保底 5s 版先行)+ CameraDirector 任务运镜 | §4.13 |
| D9 | 扫描光/候选高亮/剖层/红脉冲/热力层 五件套 | §4.8–4.12 |
| D10 | 性能收口:分块加载、三档画质、stats 实测、降级链验证 | §5.5、§8 |

> 雨/雪/四季/白鸽/光带(§4.3–4.7)属标准版,排在 M2 之后;应急推演(§4.14)属完整版。

---

## 8. 调试、性能与验收工具

### 8.1 每次提交前自查清单

```txt
□ <Stats />(@react-three/drei)常驻开发模式:FPS ≥ 60(中端)/ ≥ 30(集显)
□ r.info.render.calls ≤ 300;triangles ≤ 1.5M(控制台 window.__scene 暴露)
□ 切低档重测:pixelRatio=1、无 SSAO、无 Bloom、墨湖伪反射,FPS 必须 ≥ 30
□ 断网(DevTools offline)刷新:场景完整、贴图无 404
□ 首屏可交互 ≤ 3s(OpeningSequence 期间算可交互——可跳过)
```

### 8.2 推荐开发工具

- `@react-three/drei` 的 `Stats / PerformanceMonitor`(自动降档信号源)
- `leva`:调雾密度、光带密度、扫描速度等参数,调定后写回代码常量
- Chrome DevTools → Rendering → FPS meter;Lighthouse 只看首屏

### 8.3 模块验收口径汇总

| 模块 | 过 / 不过 |
|---|---|
| 灰盒 M0 | 抽查 10 栋位置名称全对;锚点 <5m;60FPS |
| 昼夜 | 锁 18:40 见晚霞;夜景窗灯亮;图书馆 21:30 熄 |
| 水面 | 墨湖倒影可见;其余水体有流动感;低档不掉帧 |
| 首屏 | 陌生人看 5 秒能说出"这是一座真实的校园在运行" |
| 五件套 | 输入演示指令,扫描→点亮→剖层→确认 全链有 3D 响应 |
| 性能 | §8.1 全绿 |

---

## 9. 已知坑位速查(提前免疫)

1. ExtrudeGeometry 法线翻黑 → 统一 winding,computeVertexNormals
2. 合并后点选失效 → 交互楼(L0/L1/候选)保持独立 Mesh,合并只用于沉默大多数
3. drei Html 超过 ~15 个掉帧 → 标签距离裁剪
4. Water.js 反射在夜景发黑 → 反射场景也要切光照,或低档直接伪反射
5. InstancedMesh setColorAt 不生效 → instanceColor.needsUpdate;材质开 vertexColors
6. suncalc 方位角定义:自南向西为正 → 转 three 坐标时按 §4.1 公式,勿凭直觉
7. OrbitControls 与 CameraDirector 抢相机 → 导演优先级锁,任务结束 1 秒缓释
8. 断网演示:waternormals、字体(troika 默认拉 CDN!)必须本地化 —— troika 字体用 `font=` 指向本地 woff
9. Overpass 限流 → 镜像池 + 退避;raw-osm.json 入 git,ETL 可重放不依赖网络
10. 移动端:troika Text 是首崩点,低档直接隐藏全部名牌
